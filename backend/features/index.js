const questions = require('./questions');
const aiHelp = require('./ai-help');
const verification = require('./verification');
const quiz = require('./quiz');

module.exports = {
  questions,
  aiHelp,
  verification,
  quiz
}; 